# Web UI
streamlit>=1.35.0
streamlit-image-coordinates>=0.4.0
pandas>=2.0.0

# Core computer vision
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# Progress bars
tqdm>=4.65.0

# ---------------------------------------------------------------------------
# Optional — YOLO mask generation (tools/masks.py --method yolo)
# ---------------------------------------------------------------------------
# pip install ultralytics>=8.0.0

# ---------------------------------------------------------------------------
# Optional — SAM 2 (tools/masks.py --method sam, tools/sam_segment.py)
# PyTorch is the only heavy dependency in this project — required by SAM 2.
# ---------------------------------------------------------------------------
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# pip install git+https://github.com/facebookresearch/sam2.git

# ---------------------------------------------------------------------------
# External binaries (not Python packages)
# ---------------------------------------------------------------------------
# FFmpeg >= 4.3 with v360 filter:  https://www.gyan.dev/ffmpeg/builds/
# COLMAP >= 3.8:                   https://github.com/colmap/colmap/releases
