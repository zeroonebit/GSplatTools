# Core computer vision
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# Progress bars
tqdm>=4.65.0

# ---------------------------------------------------------------------------
# Optional — mask generation
# Install these when working on tools/masks.py
# ---------------------------------------------------------------------------

# YOLOv8 object detection (drone, person, vehicle)
# pip install ultralytics>=8.0.0

# Meta Segment Anything Model (SAM) — precise pixel masks
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# pip install git+https://github.com/facebookresearch/segment-anything.git

# SAM 2 (faster, supports video)
# pip install git+https://github.com/facebookresearch/sam2.git

# ---------------------------------------------------------------------------
# Optional — COLMAP / camera pose estimation integration
# ---------------------------------------------------------------------------
# pycolmap>=0.4.0
